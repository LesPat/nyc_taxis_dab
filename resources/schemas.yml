resources:
  schemas:
    silver:
      name: ${var.silver_schema}
      catalog_name: ${var.catalog}
      comment: "Silver tables (cleansed, joined)"
    gold:
      name: ${var.gold_schema}
      catalog_name: ${var.catalog}
      comment: "Gold tables (aggregates, analytics-ready)"